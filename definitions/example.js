// Add an example to show how your package should be used
// It's a good idea to connect your package repository
// to a warehouse and ensure the package compiles, runs 
// and creates the expected results

const examplePackage = require("../");

examplePackage({
  inputDatasetName: "example_name",
  inputDatasetSchema: "example_schema"
});